---
---
<section id="artisan" class="artisan-section">
    <div class="container">
        <div class="content-grid">
            <div class="text-content">
                <span class="eyebrow">The Heritage</span>
                <h2>Forged on Namek. Assembled on Earth.</h2>
                <div class="divider"></div>
                <p>
                    Following the ancient traditions of the Dragon Clan, each component is calibrated to detect energy signatures with unprecedented precision. 
                    Our master watchmakers fuse Capsule Corp micro-engineering with the soul of a warrior.
                </p>
                <p>
                   It takes 9,000 hours to perfect the balance wheel of the Super Saiyan Complication. Perfection is not a goal; it is our power level.
                </p>
                <a href="#about" class="text-link">Read the Manga</a>
            </div>
            <div class="image-content">
                <div class="image-wrapper">
                    <img src="/images/artisan-workshop.png" alt="Capsule Corp x Namekian Atelier">
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .artisan-section {
        padding: 8rem 0;
        background: var(--color-paper);
        position: relative;
    }

    .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 2rem;
    }

    .content-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 6rem;
        align-items: center;
    }

    .eyebrow {
        color: var(--color-gold);
        text-transform: uppercase;
        letter-spacing: 0.2em;
        font-size: 0.8rem;
        display: block;
        margin-bottom: 1rem;
    }

    h2 {
        font-size: 3rem;
        color: var(--color-ink);
        margin-bottom: 2rem;
        line-height: 1.2;
    }

    .divider {
        width: 40px;
        height: 3px;
        background: var(--color-dragon-orange);
        margin-bottom: 2rem;
    }

    p {
        color: var(--color-text-muted);
        line-height: 1.8;
        font-size: 1.1rem;
        margin-bottom: 1.5rem;
    }

    .text-link {
        color: var(--color-ink);
        text-decoration: underline;
        text-decoration-color: var(--color-gold);
        text-underline-offset: 5px;
        transition: color 0.3s ease;
        font-family: var(--font-heading);
        font-style: italic;
    }

    .text-link:hover {
        color: var(--color-dragon-orange);
    }

    .image-wrapper {
        position: relative;
    }

    .image-wrapper img {
        width: 100%;
        filter: sepia(0.2) contrast(1.1);
        transition: filter 0.5s ease;
    }
    
    .image-wrapper:hover img {
        filter: sepia(0) contrast(1);
    }

    .image-wrapper::before {
        content: '';
        position: absolute;
        top: 20px;
        left: 20px;
        width: 100%;
        height: 100%;
        border: 1px solid var(--color-gold);
        z-index: -1;
    }

    @media (max-width: 900px) {
        .content-grid {
            grid-template-columns: 1fr;
            gap: 4rem;
        }

        h2 {
            font-size: 2.5rem;
        }
    }
</style>
